package com.example.networkingaf.data;
/*
import android.app.Application;

import androidx.annotation.NonNull;
import androidx.lifecycle.AndroidViewModel;
import androidx.lifecycle.LiveData;

import java.util.List;

public class OrarViewModel extends AndroidViewModel {
    private OrarRepos repository;
    private LiveData<List<DateOrar>> wholeOrar;


    public OrarViewModel(@NonNull Application application) {
        super(application);
        repository = new OrarRepos(application);
        wholeOrar = repository.getAllOrar();
    }

    public void insert(DateOrar dateOrar) {
        repository.insert(dateOrar);
    }

    public void update(DateOrar dateOrar){
        repository.update(dateOrar);
    }

    public void delete(DateOrar dateOrar) {
        repository.delete(dateOrar);
    }

    public void deleteWholeOrar() {
        repository.deleteWholeOrar();
    }

    public LiveData<List<DateOrar>> getWholeOrar() {
        return wholeOrar;
    }
}
*/